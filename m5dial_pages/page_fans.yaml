# =====================================================================
#  m5dial_pages/page_fans.yaml — page: Fans (Fanboard + HVAC)
#  FRAM M5Dial Cockpit · page package
#
#  Double ring (design.md §4), same geometry as page_gas.yaml:
#    outer 224 x 224   FANBOARD -- real, number.fan_speed_control
#    inner 200 x 200   HVAC     -- PLACEHOLDER. Sprinter's built-in
#                                  HVAC fan; no entity yet, the PCB
#                                  for it doesn't exist yet either.
#                                  Swap the entity_id once it does.
#
#  Interaction: tap a column's SET button to arm it (design.md §14,
#  g_edit_target in .m5dial_fram.yaml) -- the encoder then adjusts
#  that value in 5% steps instead of paging, until SET is tapped
#  again. Armed is shown by the SET button's border lighting up, and
#  by the footer hint. Both columns share the mechanism; only
#  FANBOARD does anything real yet -- HVAC's adjust scripts are stubs
#  (see below).
#
#  Double-click on the physical front button cycles between the two
#  columns' SET (.m5dial_fram.yaml's double-click handler -- home is
#  long-press-only here now): 1st click arms FANBOARD, 2nd click
#  switches to arming HVAC, 3rd disarms (back to navigate mode), then
#  wraps. Driven entirely off the CURRENT g_edit_target, not a
#  separate position counter (bug fix: a counter that just alternated
#  between "call act_fans_arm_board" / "call act_fans_arm_hvac" could
#  never reach disarmed -- each of those scripts only toggles ITS OWN
#  target off if IT was the one already armed, so bouncing between two
#  different targets meant neither ever saw itself armed, and the
#  encoder lock never released). Also correct if a touch tap (the
#  on-screen SET buttons) changes the armed target in between
#  double-clicks -- the cycle reads real state, not a remembered step.
#
#  Encoder feel (FANBOARD only, HVAC has no real entity to write to
#  yet): same lag fix as page_power_2.yaml (see that file's header) --
#  adjusts a local g_fans_board_pending value and redraws from it
#  immediately, only calling number.set_value once the encoder's quiet
#  for 400ms (or immediately on disarm).
#
#  on_load poll (design.md §9): poll_fans forces a fresh push from HA
#  for number.fan_speed_control on page_fans's own on_load trigger.
#  Applied to every page now, including ones that already tend to
#  update promptly -- the call is cheap, and consistency beats
#  guessing per page which integrations are fast enough to skip. NOT
#  polling number.PLACEHOLDER_hvac_fan_speed -- that entity doesn't
#  exist, forcing an update on it would just log a warning in HA for
#  nothing; add it once the real HVAC entity replaces the placeholder.
# =====================================================================

# ---------------------------------------------------------------------
#  C  Bindings
# ---------------------------------------------------------------------
sensor:
  - platform: homeassistant
    id: s_fans_board_speed
    entity_id: number.fan_speed_control
    on_value:
      - script.execute: draw_fans

  - platform: homeassistant
    id: s_fans_hvac_speed
    entity_id: number.PLACEHOLDER_hvac_fan_speed
    on_value:
      - script.execute: draw_fans

globals:
  # Page-local, same reasoning as page_power_2.yaml's
  # g_power_2_assist_pending.
  - id: g_fans_board_pending
    type: float
    restore_value: false
    initial_value: "0"

script:
  # Forces a fresh push from HA for this page's real entity -- called
  # from page_fans's own on_load trigger below. See header re: why
  # (and re: not polling the HVAC placeholder).
  - id: poll_fans
    then:
      - homeassistant.service:
          service: homeassistant.update_entity
          data:
            entity_id: number.fan_speed_control

  - id: act_fans_arm_board
    then:
      - lambda: |-
          bool was_armed = (id(g_edit_target) == 1);
          id(g_edit_target) = was_armed ? 0 : 1;
          if (!was_armed) {
            id(g_fans_board_pending) = id(s_fans_board_speed).has_state() ? id(s_fans_board_speed).state : 0.0f;
          } else {
            id(fans_board_write_debounced)->stop();
            id(fans_board_write_now)->execute();
          }
      - script.execute: draw_fans

  - id: act_fans_arm_hvac
    then:
      - globals.set:
          id: g_edit_target
          value: !lambda "return (id(g_edit_target) == 2) ? 0 : 2;"
      - script.execute: draw_fans

  # Called by .m5dial_fram.yaml's double-click handler -- see header
  # for why this reads current state instead of tracking its own
  # position: FANBOARD armed -> HVAC, HVAC armed -> disarm, anything
  # else (disarmed, or a leftover target from another page) -> FANBOARD.
  - id: dc_fans_cycle
    then:
      - lambda: |-
          int cur = id(g_edit_target);
          if (cur == 1) {
            id(fans_board_write_debounced)->stop();
            id(fans_board_write_now)->execute();
            id(g_edit_target) = 2;
          } else if (cur == 2) {
            id(g_edit_target) = 0;
          } else {
            id(g_fans_board_pending) = id(s_fans_board_speed).has_state() ? id(s_fans_board_speed).state : 0.0f;
            id(g_edit_target) = 1;
          }
      - script.execute: draw_fans

  # 5%-step, clamped 0-100. Adjusts g_fans_board_pending locally and
  # redraws immediately (see header re: lag), only calling
  # number.set_value once the encoder's quiet for 400ms.
  - id: act_fans_speed_up
    then:
      - lambda: |-
          float v = id(g_fans_board_pending) + 5.0f;
          if (v > 100.0f) v = 100.0f;
          id(g_fans_board_pending) = v;
      - script.execute: draw_fans
      - script.execute: fans_board_write_debounced

  - id: act_fans_speed_down
    then:
      - lambda: |-
          float v = id(g_fans_board_pending) - 5.0f;
          if (v < 0.0f) v = 0.0f;
          id(g_fans_board_pending) = v;
      - script.execute: draw_fans
      - script.execute: fans_board_write_debounced

  # The actual write, split from the debounce delay so disarming can
  # call it immediately (act_fans_arm_board above).
  - id: fans_board_write_now
    then:
      - homeassistant.service:
          service: number.set_value
          data:
            entity_id: number.fan_speed_control
          data_template:
            value: "{{ val }}"
          variables:
            val: !lambda "return str_sprintf(\"%.0f\", id(g_fans_board_pending));"

  - id: fans_board_write_debounced
    mode: restart
    then:
      - delay: 400ms
      - script.execute: fans_board_write_now

  # PLACEHOLDER: no HVAC fan entity yet. Wired up so target 2 already
  # works end-to-end once one exists -- give this the same
  # number.set_value shape as act_fans_speed_up/_down above, pointed
  # at the real entity_id.
  - id: act_fans_hvac_up
    then:
      - logger.log: "HVAC fan adjust: no entity yet"

  - id: act_fans_hvac_down
    then:
      - logger.log: "HVAC fan adjust: no entity yet"

  - id: draw_fans
    mode: restart
    then:
      # ---- Outer ring: FANBOARD ---------------------------------------
      - lvgl.arc.update:
          id: arc_fans_board
          value: !lambda |-
            // Armed: show the pending (locally-dialed) value, not the
            // HA-confirmed one -- see header re: lag.
            if (id(g_edit_target) == 1) return id(g_fans_board_pending);
            if (!id(s_fans_board_speed).has_state()) return 0.0f;
            return id(s_fans_board_speed).state;
          indicator:
            arc_color: !lambda |-
              #ifdef USE_API
              if (!api_is_connected()) return lv_color_hex(0x555555);
              #endif
              if (!id(s_fans_board_speed).has_state()) return lv_color_hex(0x555555);
              return lv_color_hex(0x4A9EFF);

      # ---- Inner ring: HVAC (placeholder) -----------------------------
      - lvgl.arc.update:
          id: arc_fans_hvac
          value: !lambda |-
            if (!id(s_fans_hvac_speed).has_state()) return 0.0f;
            return id(s_fans_hvac_speed).state;
          indicator:
            arc_color: !lambda |-
              #ifdef USE_API
              if (!api_is_connected()) return lv_color_hex(0x555555);
              #endif
              if (!id(s_fans_hvac_speed).has_state()) return lv_color_hex(0x555555);
              return lv_color_hex(0x9AA0A6);

      # ---- Outer column: FANBOARD --------------------------------------
      - lvgl.label.update:
          id: lbl_fans_pct_board
          text: !lambda |-
            #ifdef USE_API
            if (!api_is_connected()) return std::string("---%");
            #endif
            if (id(g_edit_target) == 1) return str_sprintf("%.0f%%", id(g_fans_board_pending));
            if (!id(s_fans_board_speed).has_state()) return std::string("---%");
            return str_sprintf("%.0f%%", id(s_fans_board_speed).state);
          text_color: !lambda |-
            #ifdef USE_API
            if (!api_is_connected()) return lv_color_hex(0x555555);
            #endif
            if (!id(s_fans_board_speed).has_state()) return lv_color_hex(0x555555);
            return lv_color_hex(0xFFFFFF);
      - lvgl.widget.update:
          id: btn_fans_set_board
          border_color: !lambda |-
            if (id(g_edit_target) == 1) return lv_color_hex(0x4A9EFF);
            return lv_color_hex(0x3C4043);

      # ---- Inner column: HVAC (placeholder) -----------------------------
      - lvgl.label.update:
          id: lbl_fans_pct_hvac
          text: !lambda |-
            #ifdef USE_API
            if (!api_is_connected()) return std::string("---%");
            #endif
            if (!id(s_fans_hvac_speed).has_state()) return std::string("---%");
            return str_sprintf("%.0f%%", id(s_fans_hvac_speed).state);
          text_color: !lambda |-
            #ifdef USE_API
            if (!api_is_connected()) return lv_color_hex(0x555555);
            #endif
            if (!id(s_fans_hvac_speed).has_state()) return lv_color_hex(0x555555);
            return lv_color_hex(0xBDC1C6);
      - lvgl.widget.update:
          id: btn_fans_set_hvac
          border_color: !lambda |-
            if (id(g_edit_target) == 2) return lv_color_hex(0x4A9EFF);
            return lv_color_hex(0x3C4043);

      # ---- Footer: edit-mode hint --------------------------------------
      - lvgl.label.update:
          id: lbl_fans_status
          text: !lambda |-
            if (id(g_edit_target) == 1 || id(g_edit_target) == 2)
              return std::string("DREHEN ZUM ÄNDERN");
            return std::string("");

esphome:
  on_boot:
    - priority: -100
      then:
        - script.execute: draw_fans

# ---------------------------------------------------------------------
#  D  Widgets
# ---------------------------------------------------------------------
lvgl:
  pages:
    - id: page_fans
      on_load:
        - script.execute: poll_fans
      bg_color: 0x000000
      bg_opa: COVER
      widgets:
        - arc:
            id: arc_fans_board
            align: CENTER
            width: 224
            height: 224
            start_angle: 135
            end_angle: 45
            min_value: 0
            max_value: 100
            adjustable: false
            arc_width: 10
            arc_color: 0x1A1C1E
            indicator:
              arc_width: 10
              arc_color: 0x555555
            knob:
              bg_opa: TRANSP

        - arc:
            id: arc_fans_hvac
            align: CENTER
            width: 200
            height: 200
            start_angle: 135
            end_angle: 45
            min_value: 0
            max_value: 100
            adjustable: false
            arc_width: 10
            arc_color: 0x1A1C1E
            indicator:
              arc_width: 10
              arc_color: 0x555555
            knob:
              bg_opa: TRANSP

        # ---- Outer column: FANBOARD ---------------------------------
        - label:
            id: lbl_fans_cap_board
            align: CENTER
            x: -${x_col}
            y: ${y_cap}
            text: "FANBOARD"
            text_font: font_12
            text_color: 0x555555
        - label:
            id: lbl_fans_pct_board
            align: CENTER
            x: -${x_col}
            y: ${y_main}
            text: "---%"
            text_font: font_24
            text_color: 0x555555
        - button:
            id: btn_fans_set_board
            align: CENTER
            x: -${x_col}
            y: ${y_row}
            width: 68
            height: 26
            radius: 13
            bg_color: 0x202124
            border_width: 1
            border_color: 0x3C4043
            widgets:
              - label:
                  align: CENTER
                  text: "SET"
                  text_font: font_16
                  text_color: 0xBDC1C6
            on_click:
              - script.execute: act_fans_arm_board

        # ---- Inner column: HVAC (placeholder) --------------------------
        - label:
            id: lbl_fans_cap_hvac
            align: CENTER
            x: ${x_col}
            y: ${y_cap}
            text: "HVAC"
            text_font: font_12
            text_color: 0x555555
        - label:
            id: lbl_fans_pct_hvac
            align: CENTER
            x: ${x_col}
            y: ${y_main}
            text: "---%"
            text_font: font_24
            text_color: 0x555555
        - button:
            id: btn_fans_set_hvac
            align: CENTER
            x: ${x_col}
            y: ${y_row}
            width: 68
            height: 26
            radius: 13
            bg_color: 0x202124
            border_width: 1
            border_color: 0x3C4043
            widgets:
              - label:
                  align: CENTER
                  text: "SET"
                  text_font: font_16
                  text_color: 0xBDC1C6
            on_click:
              - script.execute: act_fans_arm_hvac

        - label:
            id: lbl_fans_status
            align: BOTTOM_MID
            y: ${y_footer}
            text: ""
            text_font: font_12
            text_color: 0x4A9EFF
